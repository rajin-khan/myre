# experiments

Things we build and run on our own machines while figuring myre out. An experiment can be messy, short-lived, or wrong. That is the point. Each one gets its own folder when we have something to try.
